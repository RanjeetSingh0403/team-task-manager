{
  "name": "team-task-manager",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack team task management web application with authentication, projects, roles, tasks, and dashboard analytics.",
  "scripts": {
    "dev": "node scripts/dev-help.mjs",
    "install:all": "node scripts/install-all.mjs",
    "build": "node scripts/build.mjs",
    "start": "npm start --prefix server"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
